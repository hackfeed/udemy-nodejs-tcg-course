extends ../layouts/main-layout.pug

block content
    if orders.length <= 0
        h1 Nothing there!
    else
        ul
            each order in orders
                li
                    h1 ##{order._id}
                    ul
                        each item in order.products
                            li #{item.productData.title} (#{item.quantity})

